# Run whisper from terminal

Simple script to run whisper from the terminal.

Uses `sounddevice` to access the microphone, `scipy` to process the .wav file, `pyperclip` to access the clipboard and `openai` to call whisper. It assumes that a openai key has been set up as a global variable.
